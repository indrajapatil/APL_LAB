# Copy this file to `.env` and fill in the real values. Do NOT commit your real `.env`.
# Example placeholders for local development or for reference.

# MongoDB connection string (Atlas or local). Example Atlas format:
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGO_URI=mongodb://127.0.0.1:27017/finedine

# Admin secret used by the admin panel. Use a long random string in production.
ADMIN_SECRET=change_me_to_a_strong_secret
